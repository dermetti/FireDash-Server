"""Local-only developer/testing tools (gitignored).

This package only exists so the ``fake_ipad`` acceptance client can be run
as ``python -m tools.fake_ipad`` from the repository root and imported cleanly
by tooling such as mypy.
"""

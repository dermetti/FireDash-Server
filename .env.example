# Development-only example. Do not use these values in production.
DJANGO_SETTINGS_MODULE=config.settings.development
DJANGO_SECRET_KEY=replace-with-a-unique-development-secret
POSTGRES_DB=fire_backend
POSTGRES_USER=application_runtime
POSTGRES_PASSWORD=replace-with-a-development-password
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
